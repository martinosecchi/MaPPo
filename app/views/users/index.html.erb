<h1>Listing Users</h1>
<div class="hero-unit">
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %> signed up <%= user.created_at.to_date %>
    </li>
  <% end %>
</ul>
</div>

<%= link_to "Back", :back %>